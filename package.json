{
  "name": "fm-tool",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "rundev": "concurrently -n backend,frontend -c green,cyan \"npm:dev --workspace backend\" \"npm:dev --workspace frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
